#include <stdio.h>

int main() {
  int num1=7;
  int num2=90;
  if(num1<num2){
      printf("%d is the smallest",num1);
  }else{
      printf("%d is the smallest",num2);
  }

    return 0;
}